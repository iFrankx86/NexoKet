# NexoKet Environment Variables Template
# Copia este archivo a .env y completa con tus credenciales reales
# NUNCA commitees el archivo .env a Git

# MongoDB Configuration
MONGODB_URI=mongodb+srv://usuario:password@cluster0.mongodb.net/?retryWrites=true&w=majority
MONGODB_DATABASE=NexoKet

# Application Settings
APP_ENV=development  # development, production, testing
APP_PORT=8080

# Logging
LOG_LEVEL=INFO  # DEBUG, INFO, WARN, ERROR

# Backup
BACKUP_ENABLED=true
BACKUP_INTERVAL_HOURS=24
BACKUP_RETENTION_DAYS=7

# Security
SESSION_TIMEOUT_MINUTES=30

# Instrucciones:
# 1. Renombra este archivo a .env
# 2. Completa las variables con valores reales
# 3. Agrega .env al .gitignore (ya debe estar)
# 4. Nunca compartas este archivo
